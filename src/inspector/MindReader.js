import CellWidget from './CellWidget.js';
import NetWidget from './NetWidget.js';

export default class MindReader {
	constructor(canvas, critter) {
		let cellWidget = new CellWidget(canvas, critter);
		// let netWidget = new NetWidget(canvas, critter);
  }
}
